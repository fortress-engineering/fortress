{
  "$schema": "../../../../schemas/v1/change.schema.json",
  "schema_version": 1,
  "id": "CHG-SCHEMA-0005",
  "status": "archived",
  "objective": "Make the standard manifest's declared local $schema reference valid under its own additional-properties contract.",
  "baseline": {
    "commit": "eb40239949dd13bb95b65a871fa67e6cbab3cb8c",
    "branch": "dev"
  },
  "authority_refs": [
    "docs/architecture/contracts.md",
    "docs/standard/documentation.md",
    "schemas/v1/standard-manifest.schema.json"
  ],
  "scope": [
    "standard manifest schema self-description compatibility",
    "standard schema registry regression evidence"
  ],
  "checks": [
    { "command": "JSON Schema 2020-12 validation", "status": "passed", "summary": "All 11 schemas passed metaschema checks and 18 authoritative or positive instances validated against their local declared schemas." },
    { "command": "cargo fmt --all --check", "status": "passed", "summary": "The complete Rust workspace is canonically formatted." },
    { "command": "cargo clippy --workspace --all-targets --all-features -- -D warnings", "status": "passed", "summary": "The complete Rust workspace passed warnings-denied Clippy analysis without exemptions." },
    { "command": "cargo test --workspace --all-targets --all-features", "status": "passed", "summary": "All 44 tests passed, including generalized standard instance-schema compatibility evidence." },
    { "command": "RUSTDOCFLAGS=-D warnings cargo doc --workspace --all-features --no-deps", "status": "passed", "summary": "Workspace documentation built with warnings denied." }
  ],
  "decisions": [],
  "deferred_capabilities": [
    "native complete JSON Schema 2020-12 validation in fortress-core",
    "schema-reference link checking exposed through the CLI"
  ],
  "result": {
    "certification_claim": "NOT CERTIFIED",
    "commit": "30a57fd86dd86373b7529d60878161d54120a99b",
    "note": "The schema repair commit is recorded exactly. A valid schema contract and passing local checks do not create certification evidence."
  }
}

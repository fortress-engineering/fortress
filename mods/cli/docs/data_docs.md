# CLI Data

`Cargo.toml` declares explicit library, binary, and testing targets.
`command_schema_v1.json` and `commands.json` define the typed command contract
and its currently implemented instances.

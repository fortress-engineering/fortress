# CLI Testing Module

This atomic testing Module owns process-level verification of CLI discovery,
version, audit success and failure, malformed state, deterministic JSON, and
unsupported input semantics.

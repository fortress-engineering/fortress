# Code

## Role

The Code proves every Reference Resolution requirement at its immediate parent Testing boundary.

## Execution

Cargo executes one explicit integration target against deterministic in-memory CCG fixtures, synthetic large hierarchies, structurally parsed Rust/Cargo/Markdown inputs, and the live Fortress repository.

## State

Fixtures are process-local and relocation preview is non-mutating; the live case reads the repository without altering its Module hierarchy.

## Failure Semantics

Any mismatch in identity resolution, classification, canonical location, finding normalization, stable import authority, semantic delta, expected churn, deterministic bytes, or live cleanliness fails its owning Test ID.

## Files

### [`reference_resolution.rs`](../code/reference_resolution.rs)

Verifies all Reference Resolution requirements across local and semantic references, canonical projections, Rust/Cargo boundaries, subtree moves, large-system churn, and the live Fortress repository.

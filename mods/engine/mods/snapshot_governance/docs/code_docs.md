# Snapshot Governance Code

The code builds and verifies snapshots, normalizes findings, dispatches
implemented rules, reconciles ownership, evaluates the recursive placement
grammar, extracts structured Rust test facts, validates bidirectional
traceability, and orchestrates repository audits. Rust parsing returns observed
facts and remains subordinate to Fortress semantics.

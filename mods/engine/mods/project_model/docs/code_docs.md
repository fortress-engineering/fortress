# Project Model Code

`project.rs` loads and validates the project manifest, pinned standard claim,
model paths, languages, capabilities, and observation exclusions. `feature.rs`
loads feature/requirement/test contracts and rejects invalid or duplicate
identities before rule evaluation.

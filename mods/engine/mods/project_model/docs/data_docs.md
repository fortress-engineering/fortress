# Project Model Data

Versioned schema files define project, feature, change, and certification
records. Whole-repository instances live in root Data because their scope spans
multiple sibling Modules.

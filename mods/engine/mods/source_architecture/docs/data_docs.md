# Data

## Role

The Data owns the universal Source Artifact and Source Profile schemas, the intentionally empty pre-Rust profile registry, and two draft Standard rules.

## Origin

The schemas and rules are specification-authored from the language-neutral source-artifact doctrine; the registry remains empty until independently governed language profiles are implemented.

## Semantics

Source Artifact Model v1 separates authored responsibility, observed mechanics, profile/archetype conclusions, coverage, verification references, and content/Module identity. Source Profile v1 defines how a language maps native facts into universal concerns without defining universal language syntax.

## Validity

Schema identities and versions are registry-unique, profile adapters and archetypes are explicit, region lists are closed and noncontradictory, and Rust remains truthfully PROFILE_NOT_REGISTERED.

## Lifecycle

Compatible extensions retain v1; incompatible model or profile authority changes require new schema identities. A language profile is added only with its own governed conformance work.

## Files

### [`source_model_schema_v1.json`](../data/source_model_schema_v1.json)

Defines canonical deterministic Source Artifact Model v1 derived Info.

### [`source_artifact_rule.json`](../data/source_artifact_rule.json)

Defines SOURCE-ARTIFACT-001 structural source-artifact coherence.

### [`source_profile_rule.json`](../data/source_profile_rule.json)

Defines SOURCE-PROFILE-001 universal profile validity and truthful coverage.

### [`source_profile_schema_v1.json`](../data/source_profile_schema_v1.json)

Defines the language-independent Source Profile registry and profile-owned archetype contract.

### [`source_profiles.json`](../data/source_profiles.json)

Records the canonical empty profile registry before the separately sequenced Rust File Content Profile.
